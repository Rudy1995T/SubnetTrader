# Core
httpx>=0.27,<1.0
pandas>=2.1,<3.0
numpy>=1.26,<2.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
APScheduler>=3.10,<4.0
aiosqlite>=0.19,<1.0

# Optional: Bittensor SDK (install separately if needed)
# bittensor>=7.0

# Optional: WebSocket support for FlameWire
websockets>=12.0,<14.0

# Optional: Health endpoint
fastapi>=0.109,<1.0
uvicorn[standard]>=0.27,<1.0

# Testing
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.0
